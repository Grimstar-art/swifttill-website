{
  "name": "swifttill-website-sync-server",
  "version": "1.0.0",
  "description": "Local sync server for swifttill demo website; reads/writes data to C:/SwiftTill/data",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
